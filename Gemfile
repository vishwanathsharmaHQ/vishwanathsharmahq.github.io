source "https://rubygems.org"

# Matches the exact Jekyll + plugin versions GitHub Pages uses in production.
# See: https://pages.github.com/versions/
gem "github-pages", group: :jekyll_plugins

# Optional: faster incremental rebuilds while developing
gem "webrick", "~> 1.7"
